name: Issue-Comment

on: 
 issues: 
   types:  [opened]

jobs:
  comment-with-action:
       runs-on: ubuntu-latest
       steps:
         - name: Github-Context
           run: echo '${{ toJSON(github.event) }}' | jq
           shell: bash
          


   
